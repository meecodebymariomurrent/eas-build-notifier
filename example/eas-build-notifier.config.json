{
  "services": [
    {
      "name": "telegram",
      "config": {
        "token": "token",
        "chatID": "chatID",
        "appFullName": "@test/testApp"
      }
    },
    {
      "name": "slack",
      "config": {
        "webhookUrl": "webhookUrl"
      }
    },
    {
      "name": "discord",
      "config": {
        "webhookUrl": "webhookUrl"
      }
    },
    {
      "name": "teams",
      "config": {
        "webhookUrl": "webhookUrl"
      }
    }
  ]
}
